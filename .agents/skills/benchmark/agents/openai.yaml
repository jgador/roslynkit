interface:
  display_name: "Benchmark"
  short_description: "Run reproducible repository benchmarks"
  default_prompt: "Use $benchmark to run the Codex clean-room benchmark."

policy:
  allow_implicit_invocation: false
