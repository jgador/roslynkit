interface:
  display_name: "Benchmark"
  short_description: "Run direct-worktree Codex benchmarks"
  default_prompt: "Use $benchmark to dry-run the direct-worktree Codex benchmark."

policy:
  allow_implicit_invocation: false
