interface:
  display_name: "Benchmark"
  short_description: "Run Bash-controlled native retrieval benchmarks"
  default_prompt: "Use $benchmark to dry-run the Bash-controlled text-retrieval benchmark."

policy:
  allow_implicit_invocation: false
