interface:
  display_name: "Benchmark"
  short_description: "Run or analyze native retrieval benchmarks"
  default_prompt: "Use $benchmark help to show fast benchmark presets and current cases."

policy:
  allow_implicit_invocation: false
