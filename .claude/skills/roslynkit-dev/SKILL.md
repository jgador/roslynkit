---
name: roslynkit-dev
description: Use the side-by-side prerelease RoslynKit dev tool first when semantic inspection should run against an installed prerelease build; for literal search, prose, non-C# files, or RoslynKit workspace-load failures, fall back to the terminal-native tool for the current platform.
---

The canonical skill is `.agents/skills/roslynkit-dev/SKILL.md`; its full content is inlined below. If the content is missing or replaced by a policy notice, read that file directly. Do not add normative guidance to this wrapper.

!`powershell.exe -NoProfile -Command "Get-Content -Raw '.agents/skills/roslynkit-dev/SKILL.md'"`
