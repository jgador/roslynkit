namespace IntentNavigation;

/// <summary>
/// Supplies a second source declaration so context navigation can expose partial-type alternates.
/// </summary>
public sealed partial class DiagnosticReportValidator
{
    /// <summary>
    /// Identifies this deterministic fixture validator.
    /// </summary>
    public string Name => "diagnostic-report-validator";
}
