{
  "schemaVersion": 1,
  "cases": [
    {
      "id": "daemon-disconnect",
      "intent": "Find the production declarations and focused tests that explain how a daemon transport disconnect avoids partial stdout or stderr, when standalone retry occurs, and which failures do not retry.",
      "query": "daemon client transport disconnect complete buffered response partial stdout stderr standalone retry infrastructure cancellation exception",
      "requiredEvidenceGroups": [
        ["src/RoslynKit/DaemonFallbackWorkspaceCommandRouter.cs"],
        ["src/RoslynKit/DaemonClient.cs", "src/RoslynKit/DaemonPipeClient.cs", "src/RoslynKit/DaemonProtocolMessages.cs"],
        ["tests/RoslynKit.Tests/DaemonFallbackProcessIntegrationTests.cs", "tests/RoslynKit.Tests/DaemonFallbackWorkspaceCommandRouterTests.cs"]
      ]
    },
    {
      "id": "workspace-generation",
      "intent": "Find the production declarations and focused tests for consistent compiler views while tracked files change, including clean-reader capacity, pending reload priority, quiet-period retry, and a second mismatch.",
      "query": "consistent compiler view tracked files change clean readers pending reload quiet period retry second mismatch",
      "requiredEvidenceGroups": [
        ["src/RoslynKit/WorkspaceDaemonSession.cs"],
        ["tests/RoslynKit.Tests/WorkspaceDaemonSessionTests.cs"]
      ]
    },
    {
      "id": "stale-search-index",
      "intent": "Find the production declarations and focused tests for serving a coherent stale search partition during refresh while preventing partial index updates from becoming visible.",
      "query": "SearchCommandService SearchAsync RefreshAsync stale partition writer lease coherent transaction committed visibility",
      "requiredEvidenceGroups": [
        ["src/RoslynKit/SearchCommandService.cs", "src/RoslynKit/RoslynCommandExecutor.cs"],
        ["src/RoslynKit/SqliteSearchIndex.cs", "src/RoslynKit/SqliteSearchIndexWriterLease.cs"],
        ["tests/RoslynKit.Tests/SearchCommandTests.cs", "tests/RoslynKit.Tests/SqliteSearchIndexTests.cs"]
      ]
    },
    {
      "id": "symbol-comments",
      "intent": "Find the production declarations and focused tests that collect ordinary C# declaration comments and classify them as leading, body, or trailing with nested-declaration ownership and bounded descendants.",
      "query": "ordinary C# declaration comments leading body trailing nested ownership descendants truncation",
      "requiredEvidenceGroups": [
        ["src/RoslynKit/RoslynCommandExecutor.SymbolContext.cs"],
        ["tests/RoslynKit.Tests/CommandExecution/SymbolContextCommandExecutionTests.cs", "tests/RoslynKit.Tests/MarkdownFormatTests.cs"]
      ]
    },
    {
      "id": "text-only-workspace-routing",
      "intent": "Find the production declarations and focused tests that explain how text-only index and search bypass daemon routing, construct an in-process C# workspace without MSBuild, enumerate repository source files, and exclude infrastructure and build-output paths.",
      "query": "text-only standalone daemon routing DaemonFallbackWorkspaceCommandRouter executeDaemonCommand executeStandaloneCommand TextOnlyWorkspaceLoader RoslynSearchCorpusBuilder LoadTextOnlyAsync EnumerateTextOnlySourceFiles BuildAsync BuildDocumentMapAsync exclude bin obj source documents",
      "requiredEvidenceGroups": [
        ["src/RoslynKit/DaemonFallbackWorkspaceCommandRouter.cs"],
        ["src/RoslynKit/RoslynWorkspaceLoader.cs"],
        ["src/RoslynKit/RoslynSearchCorpusBuilder.cs"],
        ["tests/RoslynKit.Tests/DaemonFallbackWorkspaceCommandRouterTests.cs"],
        ["tests/RoslynKit.Tests/TextOnlyWorkspaceLoaderTests.cs"],
        ["tests/RoslynKit.Tests/RoslynSearchCorpusBuilderTests.cs"]
      ]
    },
    {
      "id": "search-ranking-pipeline",
      "intent": "Find the production declarations and focused tests that explain how identifier-shaped search queries are tokenized, converted to prefix terms, ranked by query-term coverage before Best Matching 25 relevance, and ordered deterministically while retaining strong test evidence.",
      "query": "search query tokenizer PascalCase underscore prefix matching term coverage member namespace deterministic ranking tie balanced test results",
      "requiredEvidenceGroups": [
        ["src/RoslynKit/SearchQueryTokenizer.cs"],
        ["src/RoslynKit/SqliteSearchIndex.cs"],
        ["src/RoslynKit/SearchCommandService.cs"],
        ["tests/RoslynKit.Tests/SearchQueryTokenizerTests.cs", "tests/RoslynKit.Tests/SqliteSearchIndexTests.cs"]
      ]
    }
  ]
}
